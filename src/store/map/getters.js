export const userLocation = (state) => {
    return state.userLocation
}

export const api = (state) => {
    return state.api
}

export const map = (state) => {
    return state.map
}

export const hasMap = (state) => {
    return !!state.map
}
