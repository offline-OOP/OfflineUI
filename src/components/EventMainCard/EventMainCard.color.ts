import CardScheme from '../Card/Card.color';
import ColorSchemeManager from '../../colors/ColorSchemeManager';

export const EventMainCardScheme: ColorSchemeManager = CardScheme

export default EventMainCardScheme
