import ColorSchemeManager from '../../colors/ColorSchemeManager';
import {ACCENT} from '../../colors/colors';

export const PreviewCardScheme: ColorSchemeManager = new ColorSchemeManager(ACCENT)
PreviewCardScheme.write(ACCENT, 'text-accent')

export default PreviewCardScheme
