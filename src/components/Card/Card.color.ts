import ColorSchemeManager from '../../colors/ColorSchemeManager';
import {WHITE}  from '../../colors/colors';


export const CardScheme: ColorSchemeManager = new ColorSchemeManager(WHITE)
CardScheme.write(WHITE, 'text-black')

export default CardScheme
